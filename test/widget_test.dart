import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Application skeleton test', (WidgetTester tester) async {
    // Tests futurs de l'interface avec mocks de la base de données
    expect(true, true);
  });
}
